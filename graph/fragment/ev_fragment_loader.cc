#include "graph/fragment/ev_fragment_loader.h"

namespace graph {

void EVFragmentLoader::LoadFragment(unique_ptr<IFragment> &fragment, const GraphSpec &graph_spec) {
  LOG(INFO) << "YES";
}

} // namespace graph